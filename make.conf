# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
COMMON_FLAGS="-march=skylake -O2 -pipe"

CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"

MAKEOPTS="-j7"
ACCEPT_LICENSE="*"
USE="nvidia elogind X alsa pulseaudio jpeg branding -systemd"
INPUT_DEVICES="evdev keyboard mouse wacom"
VIDEO_CARDS="nvidia"
EMERGE_DEFAULT_OPTS="--verbose --ask --quiet"

# NOTE: This stage was built with the bindist Use flag enabled
PORTDIR="/var/db/repos/gentoo"
DISTDIR="/var/cache/distfiles"
PKGDIR="/var/cache/binpkgs"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C

GENTOO_MIRRORS="https://mirrors.dotsrc.org/gentoo/"
GRUB_PLATFORMS="efi-64"

FEATURES="sign"
PORTAGE_GPG_KEY="998EF59567B28371C43AC9F7A8C7E7A801705608"
SIGNED_OFF_BY="Martin Fredin fredin@gmail.com"

